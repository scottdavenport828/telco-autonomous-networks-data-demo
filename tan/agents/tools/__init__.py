"""Tool functions exposed to the agents. Each module is a logical group:

* incident_tools — CRUD on the `incidents` Delta table
* network_tools — RAN diagnostic helpers (cell-trace stats, uplink metrics)
* rule_tools — Vector Search lookups (RCA rules, prior incidents)
"""
